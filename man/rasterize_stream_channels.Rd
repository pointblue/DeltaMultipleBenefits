% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rasterize_stream_channels.R
\name{rasterize_stream_channels}
\alias{rasterize_stream_channels}
\title{Rasterize stream channels}
\usage{
rasterize_stream_channels(
  x,
  template,
  min_length = NULL,
  dir = NULL,
  SDM = "tima",
  landscape_name = "baseline"
)
}
\arguments{
\item{x}{object of class sf, or file path to an object than can be read by
\code{sf}}

\item{template}{SpatRaster to be used as a template for rasterizing stream
channel data}

\item{min_length}{Optional; minimum length of stream channel to consider}

\item{dir}{Optional string representing directory passed to
\code{\link[terra:writeRaster]{terra::writeRaster()}}, as (\code{dir/SDM/landscape_name}). See Details.}

\item{SDM}{The name of intended species distribution model; by default
\code{"tima"}}

\item{landscape_name}{The name of the landscape scenario represented; by
default \code{"baseline"}}
}
\value{
SpatRaster
}
\description{
Convert stream channel line data to rasters for use with "tima" models
}
\details{
This function provides support for converting the California Aquatic
Resources Inventory (CARI) Streams (lines) data set to a rasterized
representation for calculating proportion cover on multiple spatial scales.
It uses the same criteria used in the original analysis, lines labeled as
'not shown' or 'Fluvial Subsurface' were excluded. It also provides an option
to exclude lines shorter than a minimum length (in meters), which was set to
1 in the original analysis.

The CARI data are freely available for download from
\href{https://www.sfei.org/data/california-aquatic-resource-inventory-cari-gis-data}{SFEI}
and
\href{https://filelib.wildlife.ca.gov/Public/BDB/GIS/BIOS/metadata/DS2836.html}{CDFW}.

By default, this layer will be assumed applicable to the \code{tima} SDM models
and assigned as the "baseline" representation of stream channels unless the
\code{SDM} or \code{landscape_name} parameters are otherwise specified. This function
can be re-run for alternative scenarios (or the resulting files simply copied
to the appropriate directory) if stream channel locations and densities will
not change in the alternative scenario. Otherwise, the result of this
function can be manipulated to represent changes to channel locations under
alternative scenarios.
}
